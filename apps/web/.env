# 各环境共用的默认值。个人本地覆盖写在 .env.local（不纳入版本控制）。
#
# 安全提示：Vite 会把 VITE_ 前缀的变量内联进构建产物，等同公开。
# 所以这里不要放 VITE_API_TOKEN —— 一个"公开的只读 token"和"直接给
# Strapi 的 Public 角色开只读权限"暴露面完全相同，却多了个要轮换、会
# 泄漏在 JS 里的密钥。正确做法是后台 Public 角色只勾 article 的
# find / findOne，前端不带 Authorization 头。
#
# 任何写操作（联系表单提交等）必须走服务端，写权限 token 只能放服务端
# 环境变量，前端永远不碰。

# 请求基地址。默认相对路径，走同源：开发期由 vite proxy 转发，线上由 nginx 反代。
VITE_API_BASE=/api
