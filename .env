# 各环境共用的默认值。个人本地覆盖写在 .env.local（不纳入版本控制）。
#
# 安全提示：Vite 会把 VITE_ 前缀的变量内联进构建产物，等同公开。
# VITE_API_TOKEN 只能放 Strapi 的只读 public token；
# 任何需要写权限的接口（如联系表单提交）必须走后端代理，不能前端直连。

# 请求基地址。默认相对路径，走同源：开发期由 vite proxy 转发，线上由 nginx 反代。
VITE_API_BASE=/api
